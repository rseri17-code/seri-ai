# Agent Operating Guide — seri.ai

- **Codex** / **Claude** read this + `NORTH_STAR.md` + `CLAUDE_HANDOFF.md` + **`PROJECT_LEAD_ASSIGNMENTS.md` (ACTIVE)**.
- `git fetch` before work. Full `npm test` && `npm run build` before push.
- Claude = visitor-facing strings. Codex = not strings (structure, a11y, validators, content-data).
- Merging `claude/*` → main is Ravikanth's call.

## SESSION HANDOFF — 2026-09-01 (Project Lead)

### Just closed
- `/about` → `/background` **308 live**
- About first screen + calm path **live**

### Highest ROI next (execute in order)

1. **Claude P0:** Home (and About card) identity **focus line** under portrait: keep Senior Technical Lead – AIOps & Observability; add *Production AI systems for enterprise operations*. Do not change frozen H1.
2. **Claude P0:** Agent **evaluation & observability** chapter on `/background` and/or `/work`.
3. **Claude P0:** **Essay depth** behind “Read the essay” (Ravikanth edits).
4. **Codex:** Verify/finish Ask a11y + Work tap targets if anything remains from redirect branch.
5. **Codex P1:** Ask **corpus prose**.
6. **Ravikanth:** essay confirm, external reviews, Sentinalai, domain.

Full detail: `PROJECT_LEAD_ASSIGNMENTS.md`.

Hero H1 frozen. TIAA intentional. No redesign cycles.
